: /*Input two numbers from user, pass these numbers to a function named as 
"calculaterRemainder", which displays their remainder and quotient separately
on screen*/
#include <iostream>
#include <string>
using namespace std;

void displayStudentInfo(int regNo, string grade, float cgpa) {
    cout << "Registration number: " << regNo << endl;
    cout << "Grade: " << grade << endl;
    cout << "CGPA: " << cgpa << endl;
